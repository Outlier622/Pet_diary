[pytest]
testpaths = tests
addopts = -q -ra --disable-warnings --cov=. --cov-report=term-missing --cov-fail-under=70

markers =
    slow: mark test as slow

filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
